import React from 'react';

const setBackgroundImg = (urlImg) => {
    const value = document.getElementById('setBackgrogundImg');
    // console.log(value);
    // document.body.style.backgroundImage = `url(${urlImg})`;
}

export default setBackgroundImg;